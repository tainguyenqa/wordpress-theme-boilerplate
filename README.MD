# WordPress Boilerplate

Developers should work from the theme template already defined in this project:

`wp-content/themes/wordpress-boilerplate`

Do not create a new theme or duplicate a full set of theme files. Update the existing template files in place unless the task explicitly requires a new theme.
