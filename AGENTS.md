# WordPress Boilerplate

Developers should work directly on the theme template already defined in this project:

`wp-content/themes/wordpress-boilerplate`

following these steps:
1. rename the theme folder to `theme-name`
2. replace all of comment references in `style.css`, `functions.php` to match the theme name
3. start working directly on this theme