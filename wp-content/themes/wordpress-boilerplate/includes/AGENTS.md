# Includes

This folder contains PHP files that are included in `functions.php`. It is used to organize theme logic, such as custom functions, shortcodes, and post type registrations, into separate files for better maintainability.

Each file should be self-contained and contain only the code necessary to perform a specific task following SOLID principles.
