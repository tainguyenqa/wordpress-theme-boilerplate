# ACF JSON

This folder is used by Advanced Custom Fields (ACF/SCF) to save and load field group configurations in JSON format. 
This allows for version control of your custom fields and automatic synchronization across different environments.

All the ACF define MUST stay inside this folder. 

Each page SHOULD have its own ACF define.
